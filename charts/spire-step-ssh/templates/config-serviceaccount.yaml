{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "spire-step-ssh.serviceAccountName" . }}-config
  labels:
    {{- include "spire-step-ssh.labels" . | nindent 4 }}
    component: config
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
