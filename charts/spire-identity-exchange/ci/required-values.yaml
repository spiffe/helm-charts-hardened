spire-identity-exchange:
  enabled: true
  tls:
    externalSecret:
      enabled: true
      secretName: spire-identity-exchange
    rest:
      enabled: true
  auth:
    plugins:
      k8s_psat:
        config:
          audiences:
            - spire-identity-exchange
          allowedServiceAccounts:
            - default/default
